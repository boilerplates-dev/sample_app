<% if current_page.last? %>
  <li class='disabled'>
    <%= content_tag(:span, raw(t 'views.pagination.last')) %>
  </li>
<% else %>
  <li>
    <%= link_to raw(t 'views.pagination.last'), url, remote: remote %>
  </li>
<% end %>
